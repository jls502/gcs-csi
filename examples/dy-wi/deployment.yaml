apiVersion: apps/v1
kind: Deployment
metadata:
  name: gcscsi-test-dy-nokey
spec:
  selector:  # 选择器定义
    matchLabels:
      app: gcscsi-test-dy-nokey
  template:
    metadata:
      labels:  # Pod 标签
        app: gcscsi-test-dy-nokey
    spec:
      containers:
      - name: writer
        image: busybox
        command:
        - sleep
        - infinity
        volumeMounts:
        - name: csi-gcs-pvc-dy-nokey
          mountPath: /data
      volumes:
      - name: csi-gcs-pvc-dy-nokey
        persistentVolumeClaim:
          claimName: csi-gcs-pvc-dy-nokey
