apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-gcs-pvc-dy-nokey
  annotations:
    gcs.csi.shein.dev/bucket: gcscsi-test
spec:
  storageClassName: csi-gcs-dy-nokey
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
