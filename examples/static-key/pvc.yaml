apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-gcs-pvc-key
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-gcs-test-sc-key
