apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-gcs-pvc-nokey
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-gcs-test-sc-nokey
